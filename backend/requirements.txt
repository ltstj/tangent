fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
numpy==2.2.1
# Phase 2: Supabase Postgres catalog (pgvector). psycopg-pool hands each
# threadpooled request its own connection instead of serializing on one.
psycopg[binary]==3.3.5
psycopg-pool==3.3.1
# Phase 2: local synopsis embeddings (all-MiniLM-L6-v2, 384-dim).
# sentence-transformers pulls torch, so first install is large and slow.
sentence-transformers==6.0.1
pgvector==0.5.0

